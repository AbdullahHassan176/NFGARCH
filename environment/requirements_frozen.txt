# Frozen Python package versions for exact reproducibility
# Generated via: pip freeze > environment/requirements_frozen.txt
# 
# NOTE: This file should be regenerated after setting up a working environment
# to capture exact versions of all installed packages (including transitive dependencies)
#
# To install exact versions:
# pip install -r environment/requirements_frozen.txt
#
# To update this file:
# pip freeze > environment/requirements_frozen.txt

# Core dependencies (minimum versions specified in requirements.txt)
# Actual installed versions should be captured here for reproducibility

numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0
matplotlib>=3.4.0
seaborn>=0.11.0
torch>=1.9.0
torchvision>=0.10.0
pyyaml>=5.4.0
openpyxl>=3.0.0
plotly>=5.0.0
nflows>=0.14.0

# NOTE: After installing packages, run:
# pip freeze > environment/requirements_frozen.txt
# to capture exact versions for reproducibility




